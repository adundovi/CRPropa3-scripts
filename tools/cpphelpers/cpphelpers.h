#include <vector>

std::vector<int> file2histogram(const char*,
                                const int,
                                const double,
                                const double,
                                const double);
